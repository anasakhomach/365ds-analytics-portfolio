# Naming Conventions

- Bronze tables preserve source entity names without the `365_` prefix.
- Silver tables use semantic names for cleaned entities and extracts.
- Gold tables use `mart_` prefixes and dashboard-ready grains.
- Boolean flags use integer `0` and `1` values to match the course brief.
- User type labels are `All`, `Free`, and `Paid`.
